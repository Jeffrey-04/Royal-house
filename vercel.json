{
  "buildCommand": "npm run build && node scripts/generate-static.mjs",
  "outputDirectory": "dist/static",
  "framework": null,
  "rewrites": [
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
